// Use the eu-0 gemini 2a node as default. We can let the user add their own node later.
export const DEFAULT_NODE = "eu-0.gemini-2a.subspace.network" 
export const DEFAULT_NODE_WEBSOCKET_URI = `wss://${DEFAULT_NODE}/ws`;